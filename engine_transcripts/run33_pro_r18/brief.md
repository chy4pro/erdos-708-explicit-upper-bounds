You are leading a research campaign. Assume a complete proof of the target exists and find it; work as an aggressive multi-agent team with an explicit route table and adversarial audit; do not search the internet; do not use earlier conversations. Partial progress is not the deliverable; return only when a complete proof has survived your own adversarial audit; if nothing survives, return only the strongest rigorously proved derivation as numbered lemmas, its exact remaining gap as a precise statement, and every machine-checkable object you built.

# Target: the fractional hinge inequality with an absolute threshold below 17 (Erdős–Surányi function g(n))

Setting (as before). Atom system: finitely many prime powers p^j (j ≥ 1) with weights α_{p,j} ≥ 0 and Σ_j α_{p,j} ≤ 1 for every prime p; S(n) = Σ_{p,j} α_{p,j}[p^j | n]. For m ≥ 1 and a window I = {x+1,…,x+m}, the hinge inequality with threshold c is
   (H_c)   Σ_{k=1}^{m} (S(k) − c)^+ ≤ Σ_{b∈I} (S(b) − 1)^+ .
Proved and kernel-checked: (H_17) for all atom systems, hence g(n) ≤ 33n. Each unit of c is worth n in the bound g(n) ≤ (c+16)n. Targets: T1 (H_c) for the smallest c you can prove completely (c = 4 ⇒ 20n; c = 8 ⇒ 24n; anything < 17 counts); T2 (equal rank) an explicit atom system, m, window violating (H_c) for a specific c (exact arithmetic; we re-check) — a counterexample to (H_2) or (H_3) would be a result.

## The proof of (H_17) you must start from (all steps checked; the constants have little slack, so a smaller c needs structural changes, not tuning)
Constants Q = 2^16, H* = 1025/1024, γ = 65536/65535, K = 35/16, coefficient 24.
Lemma 1: for u_i ∈ [0,1], (Σu_i − (r−1))^+ ≤ e_r(u) and (Σu_i − 7/2)^+ ≤ e_4(u)/2 (multi-affine vertex check); moment bound Σ_{n≤N} e_r((f_p(n))_p) ≤ N H^r/r! (coprime prime powers of distinct primes, ⌊N/d⌋ ≤ N/d); domination ⇒ H(f) ≤ H(g).
Lemma 2: split S = S_0 + (large atoms p^j > m/Q); if a large atom divides k ≤ m then the cofactor is < Q so ω(k) ≤ 7 and (S(k) − c)^+ = (S_0(k) − c)^+ for c ≥ 7; (H_17) is trivial for m < 2^48 (ω ≤ 12). [For c < 7 this lemma must be redone: the large-atom points can carry S up to 7.]
Lemma 3 (dense, H = H(S_0) ≥ H*): V = (H*/H) S_0; Σ_{k≤m} V ≥ (1 − 1/Q) m H*; Σ(V − 7)^+ ≤ m H*^8/8!; (1−1/Q)H* − H*^8/8! > 1 ⇒ Σ min(S_0,17) ≥ Σ min(V,7) ≥ m ⇒ the affine certificate S_0(n) − 1 gives (H_17). [For smaller c the dense branch needs Σ min(S_0, c) ≥ m, i.e. a mean threshold adapted to c and a sharper concentration than the eighth moment.]
Lemma 4 (sparse, H < H*): round cumulative weights down to dyadic t = 2^{-h}; retain a level (q,t) iff q^{3/t} ≤ m; b_p(n) = largest retained t with q | n; B = Σ b_p ≤ S_0; H(B) ≤ H; the unretained levels lose at most 6 in total (Σ 3 v_p(k) log p / log m ≤ 3 log k / log m over the unretained primes, doubled), so S_0(k) ≤ 2B(k) + 6 and Σ(S_0 − 17)^+ ≤ 2 L_B with L_B = Σ_{k≤m}(B(k) − 11/2)^+; retained moduli ≤ m^{1/3}.
Lemma 5 (carriers): for hot k (B(k) > 11/2) order the effective levels by decreasing value; the carrier is the shortest prefix of mass > 1; its mass is exactly 1 + θ (θ = last value, dyadic), P_C ≤ m^{(1+θ)/3} ≤ m^{2/3}; M_C = Σ (B(k) − 11/2) over k with carrier C; ν_C = ⌊m/P_C⌋; c_C = M_C/ν_C; Σ_C M_C = L_B; c_C ≤ ε_ℓ where θ = 1/ℓ, ε_1 = γH*^4/(2·4!), ε_ℓ = γ ℓ^{r−1} H*^r/r! with r = 9ℓ/2 (residual hinge (Σ b_p(u) − (9/2 − θ))^+ with u_p = b_p(u)/θ ∈ [0,1] and the moment bound over u ≤ ν_C).
Lemma 6 (certificate): F(n) = 24 Σ_C c_C [P_C | n](1 − U_C(n)/K), U_C(n) = Σ_{p∤P_C} min(b_p(n), θ_C); negative moduli P_C p^j ≤ m; carriers with last value θ = 1/ℓ dividing n number at most C_ℓ(N) = [X^{ℓ+1}](1 + X + X² + X⁴ + … + X^ℓ)^N where N = #{p : b_p(n) ≥ θ}; U_C ≥ max(0, Nθ − 1 − θ); B(n) − 1 ≥ max(θ, Nθ − 1); hence F(n) ≤ (24 Σ_ℓ ε_ℓ R_ℓ)(B(n) − 1)^+ with R_ℓ = max_N C_ℓ(N)(1 − max(0,(N−ℓ−1)/ℓ)/K)^+ / max(1/ℓ,(N−ℓ)/ℓ).
Lemma 7: 24 Σ_ℓ ε_ℓ R_ℓ < 4/5 (finite scales ℓ ≤ 64 exactly: 51/100, 6/25, 3/100, 1/250, 1/2000, 1/16000, 1/100000; tail ℓ ≥ 128 < 1/100 via C_ℓ(N) ≤ 4^{ℓ+1} f^N with f = 2633/2000, ε_ℓ ≤ (γ/ℓ)ζ^ℓ, ζ = (2EH*/9)^{9/2}, E = 87/32 > e, and the exact inequalities 4^16 f^51 (2EH*/9)^72 < 1, 16 f^4 (2EH*/9)^9 < 9/16). Margins: 0.7946 vs 0.8; the first base inequality is at 0.99365.
Lemma 8: Σ_{b∈I} F(b) ≥ 24(1 − 2γH*/K) L_B = (62248/30583) L_B (N_I(P_C) ≥ ν_C; negative moduli ≤ m so N_I ≤ 2m/(P_C q); m/(P_C ν_C) ≤ γ; mean of U_C's increments ≤ H*).
Lemma 9: Σ(S − 17)^+ ≤ 2 L_B ≤ (30583/31124)(4/5) Σ_I (B − 1)^+ ≤ Σ_I (S − 1)^+.
Where the 17 comes from: 17 = 6 (rounding loss) + 2·(11/2) with 11/2 = 1 + θ(≤1) + 7/2 (the residual hinge order), and the factor 2 from S_0 ≤ 2B + 6. To reach c = 8 or below you must (i) cut the rounding loss (finer rounding base, e.g. 2^{-h/2} or (1+δ)^{-h}, or retain more levels with a larger cutoff exponent, paying in the carrier count and in Lemma 8's negative moduli), (ii) cut the factor 2 (round to within (1+δ) instead of 2), (iii) lower the residual order 7/2 (carriers of mass 1+θ are already minimal; the residual hinge order can drop if the certificate coefficient is allowed to depend on B(k) more finely), (iv) redo Lemma 2/3 for small c (large-atom points with S ≤ 7 are no longer negligible when c < 7; use their own certificate), keeping the numerical constant below 1 with the window factor above 2 (or change 24/K).

## Dead routes (do not re-derive): nonnegative certificates (reflected windows); two-sided counts alone (projective planes); Bernoulli rounding / threshold averaging; clean reductions refuted on Hensley–Richards windows — test every intermediate statement on such windows and on CRT dense windows at 10^4–10^6 before using it.

## Routes (route table with advantage/weakness/obstacle/verification bridge; no route more than a quarter of the effort before a judge decision; the only progress metric is the gap sentence "what is missing for ALL atom systems, m, windows"; two unchanged ⇒ freeze and lower the target, e.g. prove c = 12 before c = 8): R1 finer rounding base (1+δ) with the exact count and the analytic tail redone; R2 certificate coefficients graded by the mass of k (several carrier prefixes per k as in Section 14's (2,3] weights, combined with the exact count); R3 a separate certificate for the large-atom points and a dense branch at a lower mean with a higher moment; R4 refutation at c = 2, 3, 4 on CRT-adapted windows (exact arithmetic). An adversarial agent stress-tests every candidate certificate on reflected windows (m!−m, m!], CRT dense windows, single/two-prime fractional systems and Hensley–Richards windows BEFORE any proof effort.

## Output contract: every lemma PROVED / CONDITIONAL (on what) / REFUTED / OPEN; complete proofs; the certificate as an explicit finite family with its pointwise feasibility proof and value computation; an exact-arithmetic verifier script for every numerical constant; final block "final claim ← lemmas ← unproved items"; the best threshold c fully proved and the resulting g(n) ≤ (c+16)n.
