You are leading a research campaign. Assume a complete proof of the target exists and find it; work as an aggressive multi-agent team with an explicit route table and adversarial audit; do not search the internet; do not use earlier conversations. Write every proof directly in your reply (files in your sandbox are invisible to me); print any verifier script verbatim in a fenced code block. Partial progress is not the deliverable; if nothing complete survives your own adversarial audit, return the strongest rigorously proved lemmas, the exact remaining gap as a precise statement, and every machine-checkable object you built.

# Target: the hinge inequality with threshold 2, by induction on the number of active primes

Setting. An atom system is a FINITELY SUPPORTED family of weights α_{p,j} ≥ 0 on prime powers p^j (j ≥ 1) with Σ_j α_{p,j} ≤ 1 for every prime p; S(n) = Σ_{p,j} α_{p,j}[p^j | n]. For m ≥ 1 and a window I = {x+1,…,x+m},
   (H_c)   Σ_{k=1}^{m} (S(k) − c)^+ ≤ Σ_{b∈I} (S(b) − 1)^+ .

THE KEY STRUCTURAL POINT. Every atom system has finitely many active primes. Therefore
   (H_2) holds for every system with at most N active primes, for every N   ⟺   (H_2) holds.
So (H_2) is exactly an induction on N, and the base case is already proved:

BASE CASE, PROVED AND REFEREED (verify it yourself before building on it). For at most FOUR active primes, write S = f_1 + f_2 + f_3 + f_4 with f_i ∈ [0,1] (pad with zeros), set g_{ij} = (f_i + f_j − 1)^+ and G = (1/3) Σ_{i<j} g_{ij}. Then
   (S − 2)^+ ≤ G ≤ (S − 1)^+ pointwise
(lower: Σ_{i<j} g_{ij} ≥ Σ_{i<j}(f_i + f_j − 1) = 3S − 6 and the sum is ≥ 0, using that each f_i lies in exactly three of the six pairs; upper: the six pairs split into three perfect matchings and (a−1)^+ + (b−1)^+ ≤ (a+b−1)^+, then average), and each pair term expands into NONNEGATIVE divisor increments: with cumulative weights A_a = Σ_{j≤a} α_{p,j}, B_b = Σ_{j≤b} α_{q,j} and φ(u) = (u−1)^+,
   β_{a,b} = φ(A_a+B_b) − φ(A_{a−1}+B_b) − φ(A_a+B_{b−1}) + φ(A_{a−1}+B_{b−1}) ≥ 0
(because φ is convex, u ↦ φ(u+δ) − φ(u) is nondecreasing), the boundary terms vanish since A_a, B_b ≤ 1, and telescoping gives g_{pq}(n) = Σ_{a,b≥1} β_{a,b}[p^a q^b | n]. Since #{b ∈ I : d | b} ≥ ⌊m/d⌋ for every modulus d, summing gives Σ_{k≤m}(S−2)^+ ≤ Σ_{k≤m} G ≤ Σ_{b∈I} G ≤ Σ_{b∈I}(S−1)^+. This was verified exhaustively over all weights in {0,1/6,…,1}^4 on the primes 2,3,5,7 for every m ≤ 210 and every window (105,884,100 instances, no violation).

WHY N = 5 IS NOT ROUTINE — two facts you must respect.
(1) The pair certificate dies at five primes: with five weights equal to 1/2, (S − 2)^+ = 1/2 while every pair term (1/2 + 1/2 − 1)^+ = 0, so the pair sum is identically zero and no positive multiple of it dominates.
(2) Triple terms do NOT automatically expand into nonnegative divisor increments: the third mixed difference of φ(u) = (u−1)^+ can be negative — with base 1/2 and three increments 1/2 it equals −1/2, and with base 0 and three increments 1 it equals −1. So a certificate Σ_T c_T (Σ_{i∈T} f_i − 1)^+ over triples is not a nonnegative combination of divisor indicators, and the interval comparison Σ_{k≤m} ≤ Σ_{b∈I} fails for it. Only second differences are automatically nonnegative.
Consequence: for N ≥ 5 either the certificate must still be built from PAIR terms (with coefficients depending on the primes, the weights or the level structure), or it must be SIGNED, in which case its value on the window has to be estimated as in the established route (positive moduli counted from below by ⌊m/d⌋, negative moduli from above by m/d + 1, which forces every negative modulus to be at most m).

## Targets, equal rank
T1: (H_2) for at most N active primes, for as large an N as you can prove — N = 5 is already a result, N arbitrary closes (H_2) and gives g(n) ≤ 4n through the kernel-checked transfer ((H_c) with real c ≥ 1 implies g(n) ≤ ⌈cn⌉ + 2n).
T2: refute (H_2). A counterexample may be assumed to satisfy all of: every atom p^j ≤ m/6; at least five active primes; m ≥ 66; positive left side; mean H = Σ α_{p,j}/p^j < (18 − √30)/7 = 1.78896…; and μ − H²/6 < 1 where μ = (1/m) Σ_{k≤m} S(k). Moreover if a real counterexample exists then a rational one exists with common denominator at most d! where d is the number of atoms (the violation is affine on each cell of the arrangement cut by the hyperplanes S(k) = 2 and S(b) = 1, so a vertex works, and the vertex solves a system with entries in {−1,0,1} and integer right-hand sides). Exhaustive exact searches have already found nothing: all weights in {0,1/2,1} on {2,3,5,7} and on {2,3,5,7,11} over every m and every window (1.3·10^9 instances), and all weights in {0,1/6,…,1} on {2,3,5,7} likewise.
T3 (fallback): (H_2) for five primes under an extra hypothesis you state precisely (for instance all weights in a fixed finite set, or at most one prime power per prime, or a bound on the number of prime powers).

## Proved tools you may use as black boxes
PRIMORIAL TRUNCATION: for an integer d ≥ 0 and Q_d = p_1⋯p_d (Q_2 = 6, Q_3 = 30, Q_4 = 210, Q_5 = 2310), keeping only the atoms p^j with p^j Q_d ≤ m leaves Σ_{k≤m}(S(k) − c)^+ unchanged for every c ≥ d and does not increase the right side; Q_d is optimal. So at c = 2 one may assume every atom is at most m/6.
DENSE BRANCH: if the m/6-truncated system has mean at least (18 − √30)/7 then (H_2) holds (rescale to V = (h_0/H)S_0, use ⌊m/q⌋ ≥ (6/7)(m/q), the moment bound Σ_{k≤m} e_r((f_p(k))_p) ≤ mH^r/r! and (Σu_i − 2)^+ ≤ e_2(u)/3, then the affine certificate S − 1). Likewise μ − H²/6 ≥ 1 implies (H_2).
FINITE PERIOD: for a fixed weighted system with D = lcm of the atom moduli it suffices to check 1 ≤ m ≤ D and 0 ≤ x < D.
KNOWN FAILURES OF THE INEQUALITY: (H_c) fails for every c < 2 — r primes in [M, √2 M) with M > 2, all weights 1, m = p_{r−1}p_r, window centred at their product, gives left side C(r,2)(2 − c) and right side r − 1, so equality at c = 2 − 2/r; at c = 2 this family has left side 0 and right side r − 1, i.e. it is NOT tight at 2 and gives no evidence about (H_2).

## Dead routes (do not re-derive): nonnegative certificates in general (refuted on reflected windows (m!−m, m!] in earlier rounds — the four-prime one above survives only because of its special structure); two-sided interval counts alone (projective planes); Bernoulli rounding and threshold averaging; any reduction that has been refuted on Hensley–Richards windows, where an interval can contain more integers free of small primes than [1,m] does. Test every intermediate statement on such windows and on CRT dense windows at 10^4–10^6 before using it.

## Routes (route table with advantage / weakness / obstacle / verification bridge; no route more than a quarter of the effort before a judge decision; the only progress metric is the gap sentence "what is missing for ALL atom systems, all m, all windows"; two unchanged gap sentences ⇒ freeze the route and lower the target): R1 pair certificates with prime-dependent or weight-dependent coefficients for N = 5, then general N; R2 a signed certificate at threshold 2 built on the pair expansion, with the window value estimated as in the established route; R3 induction by eliminating one prime (bound the contribution of the N-th prime by a system on N − 1 primes plus an error that the window absorbs); R4 exact refutation search inside the T2 region (five to eight primes, denominators up to d!, all windows by the period lemma, and the CRT and Hensley–Richards families). An adversarial agent must stress-test every candidate certificate on five equal half-weights, on reflected and CRT windows, and on the prime-cluster family BEFORE any proof effort.

## Output contract: every lemma PROVED / CONDITIONAL (on what) / REFUTED / OPEN; complete proofs in the reply; every certificate as an explicit finite family with its pointwise feasibility proof and its window value; exact-arithmetic verification of every numerical constant with the script printed verbatim; final block "final claim ← lemmas ← unproved items"; and, plainly, the largest N for which you prove (H_2) with at most N active primes, or the counterexample.
