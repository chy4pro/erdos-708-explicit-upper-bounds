You are leading a research campaign. Assume a complete proof of the target exists and find it; work as an aggressive multi-agent team with an explicit route table and adversarial audit; do not search the internet; do not use earlier conversations. Partial progress is not the deliverable; return only when a complete proof has survived your own adversarial audit; if nothing survives, return only the strongest rigorously proved derivation as numbered lemmas, its exact remaining gap as a precise statement, and every machine-checkable object you built.

# Target: the fractional hinge inequality with an absolute threshold below 97/10 (Erdős–Surányi function g(n))

Setting. Atom system: finitely many prime powers p^j (j ≥ 1) with weights α_{p,j} ≥ 0 and Σ_j α_{p,j} ≤ 1 for every prime p; S(n) = Σ_{p,j} α_{p,j}[p^j | n]. For m ≥ 1 and a window I = {x+1,…,x+m}, the hinge inequality with threshold c is
   (H_c)   Σ_{k=1}^{m} (S(k) − c)^+ ≤ Σ_{b∈I} (S(b) − 1)^+ .
Proved, refereed and kernel-checked: (H_{97/10}) for all atom systems. NEW transfer (kernel-checked): if (H_c) holds for all atom systems with a real c ≥ 1 then g(n) ≤ ⌈cn⌉ + 2n. So c = 97/10 gives g(n) ≤ 12n; c = 7 gives 9n; c = 6 gives 8n; c = 4 gives 6n. Targets: T1 (H_c) for the smallest c you can prove completely (c = 7 first — it is the natural boundary of the current architecture — then c ≤ 6); T2 (equal rank) an explicit atom system, m and window violating (H_c) for a specific c ≤ 4 (exact arithmetic; we re-check) — a counterexample to (H_2) or (H_3) would itself be a result.

## The proof of (H_{97/10}) you must start from (every step checked and formalised; constants have 3.9% total slack, the tightest single step 0.012%)
Constants Q = 2^16, H* = 1025/1024, Γ = 211/210, K = 8/3, λ = 51/10, ρ = 5/4, D0 = 47/16, T = 541/100, c = D0 + ρT = 97/10.
L1 (moments, optimised hinge): Σ_{n≤N} e_r((f_p(n))_p) ≤ N H(f)^r/r! for f_p ≤ 1; for a ≥ r−1, v = ⌊ra/(r−1)⌋, A_r(a) = (v−a)/C(v,r): (Σu_i − a)^+ ≤ A_r(a) e_r(u) on [0,1]^N (concave-in-each-coordinate ⇒ vertex check).
L2 (large atoms; dense): S_0 = atoms p^j ≤ m/Q; an omitted atom dividing k ≤ m forces ω(k) ≤ 7 (510510 > Q) hence S(k) ≤ 7, so (S(k) − c)^+ = (S_0(k) − c)^+ for c ≥ 7 — THIS is why c < 7 needs new work; dense branch H(S_0) ≥ H*: V = (H*/H)S_0, Σ min(V,7) ≥ m((1 − 1/Q)H* − H*^8/8!) > m ⇒ Σ min(S_0, c) ≥ m ⇒ (H_c) by the affine certificate S_0 − 1.
L3 (four-mantissa rounding, loss 47/16): levels 𝒟 = {1} ∪ {4,5,6,7}/2^h (h ≥ 3), ratio ≤ 5/4; q_p(t) = least power of p at which the cumulative p-part of S_0 reaches t; retain (p,t) iff q_p(t) ≤ m^{η(t)}, η = 1/3 (t > 1/2), 4t/9 (t ≤ 1/2); b_p(n) = largest retained level with q_p(t) | n; B = Σ b_p ≤ S_0, H(B) ≤ H; S_0(k) ≤ (5/4)B(k) + 47/16 (h ≤ 2 unretained levels above 1/2 contribute ≤ 1 each, the rest < (45/16)a_p with Σ a_p ≤ 1); hence Σ(S_0 − c)^+ ≤ (5/4) L_B with L_B = Σ_{k≤m}(B(k) − T)^+.
L4 (carriers; cofactor ≥ 210): hot k (B(k) > T); carrier = shortest prefix of mass > 1 in nonincreasing level order; 1 < s_C ≤ 1 + θ_C ≤ 2; P_C ≤ m^{2/3} (four-case exponent budget, max exactly 2/3); negative moduli P_C q ≤ m; outside mass at a source point > T − 2 = 3.41 > 3 with b_p ≤ 1 ⇒ ≥ 4 outside primes ⇒ k/P_C ≥ 210 ⇒ ν_C = ⌊m/P_C⌋ ≥ 210 and m/(P_C ν_C) ≤ Γ. (This replaces the m ≥ 2^48 proviso of the threshold-17 proof.)
L5 (mass-dependent coefficients): a = (T − s_C)/θ_C; c_C = M_C/ν_C ≤ ε_{θ,s} = min_{2≤r≤⌊a⌋+1} θ A_r(a)(H*/θ)^r/r! (moment sum over the true integers u ≤ ν_C, no Γ loss).
L6 (certificate + counting): U_C(n) = Σ_{p∉C} min(b_p(n), θ_C); F(n) = λ Σ_C c_C [P_C | n](1 − U_C(n)/K); levels θ = j/L (j ∈ {4,5,6,7}), mass s = 1 + d/L (1 ≤ d ≤ j); D_{j,L,d}(N) = [X^{L+d}](G_{j,L}^N − (G_{j,L} − X^j)^N), G_{j,L} = 1 + Σ_{t∈𝒟, t≥j/L} X^{Lt}; A_{j,L} = max_N Σ_d D_{j,L,d}(N) ε_{θ,1+d/L} (1 − max(0,Nθ−1−d/L)/K)^+ / max(d/L, Nθ−1); F(n) ≤ λ(A_{4,4} + Σ_{L≥8} Σ_j A_{j,L})(B(n) − 1)^+.
L7: the 29 scales L ≤ 512 sum to < 187/1000 (exact: 0.186024001631…; per L: 24680, 128711, 25773, 5665, 1025, 150, 21, 3 over 10^6).
L8 (tail L ≥ 1024): c_C ≤ (9θ/8) a0^{1/θ}, a0 = 4377/100000 (from z = 9/2: e log(9/2) > 4, exp((7/2)H*) < 133/4, (133/4)^100 < a0^100 (9/2)^441); generating series 𝒢_j(x) with (x_j, F_j) = (583/1000, 632591/500000), (321/500, 159089/125000), (69/100, 1278827/10^6), (73/100, 639137/500000); tail < 3/1000.
L9 (window): Σ_I F ≥ λ(1 − 2ΓH*/K) L_B = (718539/573440) L_B > (5/4) L_B. Closing: Σ(S − c)^+ ≤ (5/4)L_B ≤ Σ_I F ≤ (969/1000) Σ_I (B − 1)^+ ≤ Σ_I (S − 1)^+.
Where 97/10 comes from: c = D0 + ρT with D0 = 47/16 (rounding loss: two unretained levels above 1/2 at 1 each + 45/16·(1/3)), ρ = 5/4 (mantissa ratio), T = 541/100 = 1 + θ(≤ 1) + residual order (≈ 3.41 — needed for '≥ 4 outside primes ⇒ cofactor ≥ 210'). Levers: (i) eight mantissas (ratio 9/8): D0 and ρ both drop, the scale count doubles; (ii) a cheaper cofactor argument (3 outside primes ⇒ cofactor ≥ 30, Γ = 31/30, costs in L9) lets T drop by 1 — worth ρ·1 ≈ 1.1–1.25 in c; (iii) the retention exponent η (a larger cutoff retains more, cutting D0, but P_C ≤ m^{2/3} and P_C q ≤ m must survive); (iv) K and λ re-optimised jointly with the window factor; (v) for c < 7: L2 must be redone — the large-atom points (k ≤ m divisible by an omitted atom q > m/Q) can have S(k) up to 7; bound their contribution Σ_{k large}(S(k) − c)^+ by a separate certificate on the omitted atoms (each omitted atom has < Q multiples; use the two-sided counts) or lower Q; the dense branch needs Σ min(S_0, c) ≥ m at the smaller c (higher moment order via A_r(a) with a = c).

## Dead routes (do not re-derive): nonnegative certificates (reflected windows); two-sided counts alone (projective planes); Bernoulli rounding / threshold averaging; clean reductions refuted on Hensley–Richards windows — test every intermediate statement on such windows and on CRT dense windows at 10^4–10^6 before using it.

## Routes (route table with advantage/weakness/obstacle/verification bridge; no route more than a quarter of the effort before a judge decision; the only progress metric is the gap sentence "what is missing for ALL atom systems, m, windows"; two unchanged ⇒ freeze and lower the target, e.g. prove c = 8 before c = 7): R1 eight mantissas + cheaper cofactor (target c = 7); R2 the large-atom certificate for c < 7 (target c = 6); R3 joint re-optimisation of (K, λ, η, T) with the exact finite scales recomputed; R4 refutation at c = 2, 3, 4 on CRT-adapted windows (exact arithmetic). An adversarial agent stress-tests every candidate certificate on reflected windows (m!−m, m!], CRT dense windows, single/two-prime fractional systems and Hensley–Richards windows BEFORE any proof effort.

## Output contract: every lemma PROVED / CONDITIONAL (on what) / REFUTED / OPEN; complete proofs (Markdown + LaTeX, written directly in the reply, NOT only as files — files in your sandbox are not visible to me; if you also build scripts, print exact_verifier.py verbatim in a code block at the end); the certificate as an explicit finite family with its pointwise feasibility proof and value computation; an exact-arithmetic verifier for every numerical constant; final block "final claim ← lemmas ← unproved items"; the best threshold c fully proved and the resulting g(n) ≤ ⌈cn⌉ + 2n.
